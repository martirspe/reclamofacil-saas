# Default tenant assets

Estos archivos son de solo lectura y sirven como fallback si un tenant no tiene configurado su branding.

Rutas servidas:
- /assets/default-tenant/logo-light.png
- /assets/default-tenant/logo-dark.png
- /assets/default-tenant/favicon.png

No mezclar con uploads de usuarios; los archivos subidos van en `uploads/`.
