# Development container for dashboard (hot reload)
FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 4202
CMD ["sh", "-c", "npm install && npm run start:dashboard -- --host 0.0.0.0 --poll=2000"]
