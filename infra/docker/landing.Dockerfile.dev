# Development container for landing (hot reload)
FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 4200
CMD ["sh", "-c", "npm install && npm run start:landing -- --host 0.0.0.0 --poll=2000"]
