# Development container for public-form (hot reload)
FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 4201
CMD ["sh", "-c", "npm install && npm run start:public-form -- --host 0.0.0.0 --poll=2000"]
